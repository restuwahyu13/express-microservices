####################################
# KUBERNETES PODS - PROXY SERVICE
####################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: proxy
  labels:
    name: proxy
spec:
  replicas: 3
  selector:
    matchLabels:
      name: proxy
  template:
    metadata:
      labels:
        name: proxy
    spec:
      containers:
        - name: proxy-service
          image: docker.io/705471/proxy-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: '256Mi'
              cpu: '256m'
            limits:
              memory: '512Mi'
              cpu: '512m'
      restartPolicy: Always
####################################
# KUBERNETES PODS - USERS SERVICE
####################################
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
  labels:
    name: users
spec:
  replicas: 3
  selector:
    matchLabels:
      name: users
  template:
    metadata:
      labels:
        name: users
    spec:
      containers:
        - name: users-service
          image: docker.io/705471/users-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: '256Mi'
              cpu: '256m'
            limits:
              memory: '512Mi'
              cpu: '512m'
      restartPolicy: Always
####################################
# KUBERNETES PODS - ROLES SERVICE
####################################
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: roles
  labels:
    name: roles
spec:
  replicas: 3
  selector:
    matchLabels:
      name: roles
  template:
    metadata:
      labels:
        name: roles
    spec:
      containers:
        - name: roles-service
          image: docker.io/705471/roles-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: '256Mi'
              cpu: '256m'
            limits:
              memory: '512Mi'
              cpu: '512m'
      restartPolicy: Always
####################################
# KUBERNETES SERVICE - PROXY SERVICE
####################################
---
kind: Service
apiVersion: v1
metadata:
  name: proxy
  labels:
    name: proxy
spec:
  selector:
    name: proxy
  type: LoadBalancer
  ports:
    - name: proxy-service
      port: 80
      targetPort: 80
####################################
# KUBERNETES SERVICE - USERS SERVICE
####################################
---
kind: Service
apiVersion: v1
metadata:
  name: users
  labels:
    name: users
spec:
  selector:
    name: users
  type: LoadBalancer
  ports:
    - name: users-service
      port: 3000
      targetPort: 3000
####################################
# KUBERNETES SERVICE - ROLES SERVICE
####################################
---
kind: Service
apiVersion: v1
metadata:
  name: roles
  labels:
    name: roles
spec:
  selector:
    name: roles
  type: LoadBalancer
  ports:
    - name: roles-service
      port: 3000
      targetPort: 3000
